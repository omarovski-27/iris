# Rust
target/
# (Cargo.lock stays tracked — this is a binary project)

# Node / frontend
node_modules/
dist/

# Tauri
src-tauri/target/
src-tauri/gen/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Local env / keys
.env
.env.*
*.local

# Iris's own config/history, in case a local --config points them at the repo
# (the default path is outside the repo, at the platform config dir). Anchored
# to the root so this doesn't shadow crate-level files like .cargo/config.toml.
/config.toml
/history.jsonl
